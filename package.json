{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist/ && mkdir dist && wp-scripts build",
    "start": "rm -rf dist/ && mkdir dist && wp-scripts start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^16.1.3",
    "path": "^0.12.7",
    "react-select": "^4.3.1",
    "react-select-async-paginate": "^0.5.3"
  }
}
